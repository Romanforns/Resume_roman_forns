# Archivos estáticos

**resume.pdf** se genera automáticamente al ejecutar `npm run build` a partir del contenido de la página "View full CV" (/cv). No hace falta añadirlo a mano.

Para regenerar solo el PDF (después de haber hecho build): `npm run generate:pdf`.
